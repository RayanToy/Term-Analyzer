# Python 3.11 (conda env "kk")
#
# Вся морфология проекта — pymorphy3. pymorphy2 НЕ используется и НЕ должен
# быть установлен: natasha тянет его только ради MorphVocab, который здесь
# не применяется (см. src/natasha_loader.py — там подставляется заглушка).

pymorphy3>=2.0
razdel>=0.5
python-docx>=1.1
openpyxl>=3.1
pypdf>=4.0

# natasha нужна только как контекстный теггер частей речи (NewsMorphTagger)
# для второго варианта лемматизации и сравнения с pymorphy3.
natasha>=1.6

# pkg_resources для внутренних импортов natasha/slovnet
setuptools<81
